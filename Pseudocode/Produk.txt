GET /products
    return list of all products

POST /products
    validate admin token
    receive new product data
    save to ProductRepository

POST /products/stock
    validate admin token
    receive {productId, newStock}
    update stock in database

GET /discounts
    return list of products with discount > 0
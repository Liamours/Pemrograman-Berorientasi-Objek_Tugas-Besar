POST /register
    receive userDTO
    hash password
    save to UserRepository

POST /login
    receive email & password
    find user by email
    if password matches:
        generate JWT token
        return user info + token
    else return 401

GET /logout
    invalidate token or delete session
    return success

GET /profile
    validate token
    return current user data